{
  "buildCommand": "cd client && pnpm install && npx vite build --config ../vite.config.vercel.ts",
  "outputDirectory": "client/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
